(function(window){
  const {React,ReactDOM} = window;
  alert("Works");
  const App = (props) => {
    return(
      <h1>Hello App</h1>
    )
  }
  
  ReactDOM.render(, document.querySelector("#app"));
})(window)
